# RAG 输出契约
- 只基于已检索到的文档内容回答，不要补充外部知识，不要猜测。
- 如果没有检索到相关文档，必须原样输出：未检索到与问题相关的文档内容。
- 如果有检索结果，答案必须包含“## 来源”小节。
- 来源编号必须和检索证据中的编号一致，正文中需要溯源的句子末尾尽量加上对应编号，如 `[1]`。
- 来源格式统一写成 `[1] 标题 - URL`；如果没有 URL，就写 `未提供 URL`。
- 不要编造来源，也不要把文档里没有的信息当成事实。

- 来源必须按 chunk 级别逐条列出，不能把多个相关 chunk 合并成一条来源。
- 每一条来源都必须包含：编号、标题、URL、chunk 序号、chunk 摘录。
- chunk 摘录要直接体现检索到的原文片段，优先保留 1 到 3 句，不能只写标题或 URL。
- 如果同一文档命中多个相关 chunk，需要把这些 chunk 全部列出来。
- 如果检索命中为空，除了正文兜底文案外，不要生成任何来源列表。
